<%#
name: Community Restart Services - Katello Ansible Default
snippet: false
template_inputs:
- name: helper
  required: true
  input_type: user
  description: A comma separated list of commands to run to restart services
  advanced: false
  value_type: plain
model: JobTemplate
job_category: Katello via Ansible
description_format: Restart Services
provider_type: Ansible
kind: job_template
organizations:
- Default Organization
locations:
- Default Location
%>

<%= render_template(
  'Run Command - Ansible Default',
  :command => "#{input(:helper)},katello-tracer-upload".split(',').map {|split| "#{split}" }.join("\n")
) %>